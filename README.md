# Quick Input Popup Chrome Extension

这个示例是一个简易的 Chrome 插件，点击工具栏图标后会弹出类似 MetaMask 的小窗口，方便输入并保存一段文字。

## 使用方式

1. 打开 Chrome，进入 `chrome://extensions/`。
2. 打开右上角「开发者模式」。
3. 点击「加载已解压的扩展程序」，选择本项目目录。
4. 点击插件图标即可看到弹窗，输入内容后点击「保存」。

保存的内容会显示在弹窗底部，并存储在 `chrome.storage.local` 中。
